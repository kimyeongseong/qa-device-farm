# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Node
node_modules/

# ws-scrcpy build output
ws-scrcpy/dist/
ws-scrcpy/build/

# 배포본 빌드 산출물 (build/build.py). 스크립트는 커밋하고 결과물은 빼둡니다.
build/out/
build/work/
build/spec/
build/cache/

# Local runtime state (device serials, recorded macros, uploads)
device_aliases.json
device_leases.json
macros/*.json
temp_*.apk
screenshots/
scrcpy_bin/
*.log
logs/

# Local deployment config (tunnel hostnames)
static/stream-config.js

# Captured device logs -- never commit, they carry app and account data
raw_logs.txt
crash.txt
*crash*.txt
logcat*.txt

# Archives
*.zip

# Editors / OS
.idea/
.vscode/
.DS_Store
Thumbs.db
